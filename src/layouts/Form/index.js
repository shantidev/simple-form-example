import React from 'react';
import Layout, { MainContainer } from '../Layout';
import Form from '../../containers/Form';

export default () => (
  <Layout>
    <MainContainer>
      <Form/>
    </MainContainer>
  </Layout>
)