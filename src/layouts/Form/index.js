import React from 'react';
import Layout, {MainContainer} from '../Layout';
import Form from '../../containers/Form';

export default () => (
  <Layout>
    <MainContainer>
      <h1>Simple form</h1>
      <Form/>
    </MainContainer>
  </Layout>
)